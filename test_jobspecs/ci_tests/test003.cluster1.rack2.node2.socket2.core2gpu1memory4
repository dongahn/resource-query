version: 1
resources:
  - type: cluster
    count: 1
    with:
      - type: rack 
        count: 2 
        with: 
          - type: node
            count: 2
            with:
              - type: socket
                count: 2
                with:
                  - type: slot
                    count: 1
                    label: default
                    with:
                      - type: core
                        count: 2
                        exclusive: true
                      - type: gpu
                        count: 1
                        exclusive: true
                      - type: memory
                        count: 4
                        exclusive: true
# a comment
attributes:
  system:
    duration: 3600
tasks:
  - command: app
    slot:
      type: node
    count:
      per_slot: 1
